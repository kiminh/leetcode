class Solution {
public:
    int myAtoi(string str) {
        bool found_first_nw = false;
        for (auto ch : str) {
            if (ch == ' ') {
                continue;
            } else if (ch == )
        }
        
    }
};